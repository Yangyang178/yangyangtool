@import "../../styles/tool-common.wxss";

page { background-color: var(--pageBg,#F8FAFC); }

.tool-page {
  height: 100vh;
  display: flex;
  flex-direction: column;
}

.container {
  padding: var(--space-lg, 20px);
  padding-bottom: calc(var(--space-xl, 40px) + env(safe-area-inset-bottom));
}

/* ===================================
   🎯 工具头部 - 粉色主题
   =================================== */
.tool-header {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: var(--spaceXL, 24px) 0 var(--spaceLG, 20px);
}

.icon-wrapper {
  width: 80px;
  height: 80px;
  border-radius: var(--radiusXl, 24px);
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: var(--spaceMD, 12px);
  background: linear-gradient(135deg, var(--accentPink,#EC4899) 0%, #DB2777 100%);
  box-shadow: 0 8px 24px rgba(236, 72, 153, 0.3);
  position: relative;
  z-index: 2;
}

.icon-decoration {
  position: absolute;
  width: 100px;
  height: 100px;
  border-radius: 50%;
  background: var(--accentPink,#EC4899);
  opacity: 0.15;
  filter: blur(20px);
  z-index: 1;
}

.tool-icon {
  font-size: 40px;
  position: relative;
  z-index: 2;
}

.tool-title {
  font-size: var(--fontSizeXl, 22px);
  font-weight: var(--fontWeightBold, 700);
  color: var(--textPrimary,#1E293B);
  margin-bottom: var(--spaceSM, 6px);
}

.tool-desc {
  font-size: var(--fontSizeSm, 13px);
  color: var(--textSecondary,#64748B);
}

/* ===================================
   📥 输入区域
   =================================== */
.input-section {
  margin-top: var(--spaceSM, 10px);
}

.input-card {
  background: var(--cardBg,#FFFFFF);
  border-radius: var(--radiusLg, 16px);
  padding: var(--spaceXL, 24px);
  box-shadow: var(--shadowMd, 0 4px 12px rgba(0, 0, 0, 0.08));
}

.input-group {
  margin-bottom: var(--spaceXL, 20px);
}

.input-label {
  display: block;
  font-size: var(--fontSizeSm, 14px);
  font-weight: var(--fontWeightSemibold, 600);
  color: var(--textPrimary,#1E293B);
  margin-bottom: var(--spaceMD, 10px);
}

.date-picker-box {
  height: 48px;
  background: var(--pageBg,#F8FAFC);
  border-radius: var(--radiusMd, 10px);
  padding: 0 var(--spaceLG, 16px);
  display: flex;
  align-items: center;
  justify-content: space-between;
  font-size: var(--fontSizeBase, 15px);
  color: var(--textPrimary,#1E293B);
  border: 2px solid transparent;
  transition: all var(--durationFast, 0.2s) ease;
}

.date-picker-box:active {
  border-color: var(--accentPink,#EC4899);
}

.picker-icon { font-size: 18px; }

.quick-birthdays { margin-bottom: var(--spaceXL, 24px); }

.quick-label {
  display: block;
  font-size: var(--fontSizeSm, 13px);
  color: var(--textSecondary,#64748B);
  margin-bottom: var(--spaceMD, 10px);
}

.quick-btn {
  display: inline-block;
  padding: var(--spaceSM, 6px) var(--spaceMD, 14px);
  background: var(--bgTipRed,#FEF2F2);
  border-radius: var(--radiusLg, 16px);
  font-size: var(--fontSizeXs, 12px);
  font-weight: var(--fontWeightSemibold, 600);
  color: var(--accentPink,#EC4899);
  margin-right: var(--spaceSM, 8px);
  margin-bottom: var(--spaceSM, 8px);
  transition: transform var(--durationFast, 0.2s) ease;
}

.quick-btn:active { transform: scale(0.95); }

/* ===================================
   🔘 计算按钮 - 使用统一系统
   =================================== */
.calculate-btn {
  width: 100%;
  height: 52px;
  background: linear-gradient(135deg, var(--accentPink,#EC4899) 0%, #DB2777 100%);
  border-radius: var(--radiusMd, 12px);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: var(--fontSizeBase, 16px);
  font-weight: var(--fontWeightBold, 700);
  color: white;
  box-shadow: 0 4px 12px rgba(236, 72, 153, 0.4);
}

.calculate-btn:active { transform: scale(0.98); }

/* ===================================
   📊 结果展示 - 粉色渐变主题
   =================================== */
.result-section { animation: slideUp var(--durationNormal, 0.4s) ease; }

@keyframes slideUp {
  from { opacity: 0; transform: translateY(20px); }
  to { opacity: 1; transform: translateY(0); }
}

.age-display-card {
  background: linear-gradient(135deg, var(--accentPink,#EC4899) 0%, #DB2777 100%);
  border-radius: var(--radiusXl, 20px);
  padding: var(--spaceXxl, 28px);
  margin-bottom: var(--spaceXL, 20px);
  box-shadow: 0 8px 24px rgba(236, 72, 153, 0.3);
}

.age-header {
  display: flex;
  align-items: center;
  gap: var(--spaceMD, 12px);
  margin-bottom: var(--spaceXL, 20px);
}

.zodiac-emoji { font-size: 40px; }

.zodiac-info { display: flex; flex-direction: column; }

.zodiac-name {
  font-size: var(--fontSizeLg, 18px);
  font-weight: var(--fontWeightBold, 700);
  color: white;
}

.constellation-name {
  font-size: var(--fontSizeSm, 13px);
  color: rgba(255, 255, 255, 0.85);
}

.age-main {
  text-align: center;
  margin-bottom: var(--spaceXL, 20px);
}

.age-number {
  display: flex;
  align-items: baseline;
  justify-content: center;
  gap: var(--spaceMD, 8px);
}

.age-value {
  font-size: 64px;
  font-weight: var(--fontWeightBlack, 800);
  color: white;
  line-height: 1;
}

.age-unit {
  font-size: var(--fontSizeXl, 28px);
  font-weight: var(--fontWeightSemibold, 600);
  color: rgba(255, 255, 255, 0.9);
}

.detail-text {
  font-size: var(--fontSizeBase, 16px);
  color: rgba(255, 255, 255, 0.85);
  font-weight: var(--fontWeightMedium, 500);
}

.next-birthday-info {
  background: rgba(255, 255, 255, 0.15);
  border-radius: var(--radiusMd, 12px);
  padding: var(--spaceLG, 16px);
  text-align: center;
}

.countdown-days {
  font-size: 32px;
  font-weight: var(--fontWeightBlack, 800);
  color: var(--accentYellow,#F59E0B);
}

.life-stats-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--spaceMD, 12px);
  margin-bottom: var(--spaceXL, 20px);
}

.stat-card {
  background: var(--cardBg,#FFFFFF);
  border-radius: var(--radiusLg, 14px);
  padding: var(--spaceLG, 16px) var(--spaceMD, 12px);
  text-align: center;
  box-shadow: var(--shadowSm, 0 2px 8px rgba(0, 0, 0, 0.06));
}

.stat-icon {
  font-size: 24px;
  display: block;
  margin-bottom: var(--spaceSM, 6px);
}

.stat-value {
  font-size: var(--fontSizeLg, 18px);
  font-weight: var(--fontWeightBlack, 800);
  color: var(--accentPink,#EC4899);
  display: block;
  line-height: 1.2;
}

.stat-label {
  font-size: var(--fontSizeMini, 11px);
  color: var(--textTertiary,#94A3B8);
  font-weight: var(--fontWeightSemibold, 600);
  margin-top: var(--spaceXS, 4px);
  display: block;
}

/* ===================================
   🏆 里程碑 + 历史记录
   =================================== */
.milestone-section,
.history-section {
  background: var(--cardBg,#FFFFFF);
  border-radius: var(--radiusLg, 16px);
  padding: var(--spaceXL, 20px);
  box-shadow: var(--shadowSm, 0 2px 8px rgba(0, 0, 0, 0.06));
  margin-bottom: var(--spaceXL, 20px);
}

.milestone-title {
  font-size: var(--fontSizeBase, 16px);
  font-weight: var(--fontWeightBold, 700);
  color: var(--textPrimary,#1E293B);
  display: block;
  margin-bottom: var(--spaceLG, 14px);
}

.action-buttons {
  display: flex;
  gap: var(--spaceMD, 12px);
  margin-bottom: var(--spaceXL, 20px);
}

.life-progress-section {
  background: var(--cardBg,#FFFFFF);
  border-radius: var(--radiusLg, 16px);
  padding: var(--spaceXL, 20px);
  box-shadow: var(--shadowSm, 0 2px 8px rgba(0, 0, 0, 0.06));
  margin-bottom: var(--spaceXL, 20px);
}

.life-progress-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: var(--spaceLG, 16px);
}

.life-progress-title {
  font-size: var(--fontSizeBase, 16px);
  font-weight: var(--fontWeightBold, 700);
  color: var(--textPrimary,#1E293B);
}

.life-expectancy-row {
  display: flex;
  align-items: center;
  gap: 6px;
}

.life-expectancy-label {
  font-size: var(--fontSizeXs, 12px);
  color: var(--textTertiary,#94A3B8);
}

.life-expectancy-value {
  font-size: var(--fontSizeSm, 14px);
  font-weight: var(--fontWeightBold, 700);
  color: var(--accentPink,#EC4899);
}

.life-progress-bar {
  margin-bottom: var(--spaceLG, 16px);
}

.life-progress-track {
  height: 16px;
  background: var(--pageBg,#F1F5F9);
  border-radius: 8px;
  position: relative;
  overflow: visible;
}

.life-progress-fill {
  height: 100%;
  background: linear-gradient(90deg, #10B981 0%, #F59E0B 50%, #EF4444 100%);
  border-radius: 8px;
  transition: width 0.8s ease;
  position: relative;
}

.life-progress-marker {
  position: absolute;
  top: -4px;
  transform: translateX(-50%);
  z-index: 2;
}

.marker-dot {
  width: 24px;
  height: 24px;
  border-radius: 50%;
  background: var(--accentPink,#EC4899);
  border: 3px solid #FFFFFF;
  box-shadow: 0 2px 8px rgba(236, 72, 153, 0.4);
}

.life-progress-labels {
  display: flex;
  justify-content: space-between;
  margin-top: 8px;
}

.progress-born {
  font-size: var(--fontSizeXs, 11px);
  color: var(--textTertiary,#94A3B8);
}

.progress-now {
  font-size: var(--fontSizeXs, 11px);
  color: var(--accentPink,#EC4899);
  font-weight: var(--fontWeightSemibold, 600);
}

.progress-end {
  font-size: var(--fontSizeXs, 11px);
  color: var(--textTertiary,#94A3B8);
}

.life-progress-stats {
  display: flex;
  align-items: center;
  justify-content: space-around;
  padding: var(--spaceLG, 16px) 0;
}

.life-stat {
  text-align: center;
  flex: 1;
}

.life-stat-value {
  font-size: var(--fontSizeLg, 20px);
  font-weight: var(--fontWeightBlack, 800);
  color: var(--accentPink,#EC4899);
  display: block;
}

.life-stat-label {
  font-size: var(--fontSizeXs, 11px);
  color: var(--textTertiary,#94A3B8);
  font-weight: var(--fontWeightMedium, 500);
  margin-top: 4px;
  display: block;
}

.life-stat-divider {
  width: 1px;
  height: 30px;
  background: var(--borderLight,#E2E8F0);
}

.date-calc-section {
  background: var(--cardBg,#FFFFFF);
  border-radius: var(--radiusLg, 16px);
  padding: var(--spaceXL, 20px);
  box-shadow: var(--shadowSm, 0 2px 8px rgba(0, 0, 0, 0.06));
  margin-bottom: var(--spaceXL, 20px);
}

.date-calc-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.date-calc-title {
  font-size: var(--fontSizeBase, 16px);
  font-weight: var(--fontWeightBold, 700);
  color: var(--textPrimary,#1E293B);
}

.date-calc-toggle {
  font-size: var(--fontSizeXs, 12px);
  font-weight: var(--fontWeightSemibold, 600);
  color: var(--textSecondary,#64748B);
  padding: 4px 14px;
  border-radius: 16px;
  background: var(--pageBg,#F8FAFC);
  border: 1.5px solid var(--borderLight,#E2E8F0);
}

.date-calc-toggle.active {
  background: linear-gradient(135deg, #EC4899 0%, #DB2777 100%);
  color: #FFFFFF;
  border-color: transparent;
  box-shadow: 0 4px 12px rgba(236, 72, 153, 0.3);
}

.date-calc-body {
  margin-top: var(--spaceLG, 16px);
  animation: slideUp 0.3s ease;
}

.date-calc-inputs {
  display: flex;
  align-items: center;
  gap: var(--spaceMD, 12px);
  margin-bottom: var(--spaceLG, 16px);
}

.date-calc-input {
  flex: 1;
}

.date-calc-label {
  font-size: var(--fontSizeXs, 12px);
  color: var(--textSecondary,#64748B);
  font-weight: var(--fontWeightSemibold, 600);
  display: block;
  margin-bottom: 6px;
}

.date-calc-picker {
  height: 40px;
  background: var(--pageBg,#F8FAFC);
  border-radius: var(--radiusMd, 10px);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: var(--fontSizeSm, 13px);
  color: var(--textPrimary,#1E293B);
  font-weight: var(--fontWeightMedium, 500);
}

.date-calc-arrow {
  font-size: 18px;
  color: var(--accentPink,#EC4899);
  font-weight: var(--fontWeightBold, 700);
  margin-top: 16px;
}

.date-calc-btn {
  width: 100%;
  height: 44px;
  background: linear-gradient(135deg, #EC4899 0%, #DB2777 100%);
  border-radius: var(--radiusMd, 10px);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: var(--fontSizeBase, 15px);
  font-weight: var(--fontWeightBold, 700);
  color: #FFFFFF;
  box-shadow: 0 4px 12px rgba(236, 72, 153, 0.3);
  margin-bottom: var(--spaceLG, 16px);
}

.date-calc-btn:active {
  transform: scale(0.98);
}

.date-calc-result {
  background: var(--pageBg,#F8FAFC);
  border-radius: var(--radiusMd, 12px);
  padding: var(--spaceLG, 16px);
}

.calc-result-main {
  display: flex;
  align-items: baseline;
  justify-content: center;
  gap: 6px;
  margin-bottom: var(--spaceMD, 12px);
}

.calc-result-value {
  font-size: 36px;
  font-weight: var(--fontWeightBlack, 800);
  color: var(--accentPink,#EC4899);
  line-height: 1;
}

.calc-result-unit {
  font-size: var(--fontSizeLg, 18px);
  font-weight: var(--fontWeightSemibold, 600);
  color: var(--textSecondary,#64748B);
}

.calc-result-detail {
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.calc-detail-item {
  font-size: var(--fontSizeSm, 13px);
  color: var(--textSecondary,#64748B);
  text-align: center;
}

.dark-mode page { background-color: var(--pageBgNight,#0f1419); }

.dark-mode .icon-wrapper {
  box-shadow: 0 8px 24px rgba(236, 72, 153, 0.25);
}

.dark-mode .icon-decoration { opacity: 0.1; }

.dark-mode .tool-title { color: var(--textPrimary,#F1F5F9); }

.dark-mode .input-card,
.dark-mode .stat-card,
.dark-mode .milestone-section,
.dark-mode .history-section,
.dark-mode .life-progress-section,
.dark-mode .date-calc-section {
  background: var(--elevatedBg,#232d42);
}

.dark-mode .input-label,
.dark-mode .milestone-title,
.dark-mode .life-progress-title,
.dark-mode .date-calc-title { color: var(--textPrimary,#F1F5F9); }

.dark-mode .date-picker-box { background: var(--cardBg,#1a2035); }

.dark-mode .stat-value { color: var(--accentPink,#F472B6); }

.dark-mode .life-progress-track {
  background: var(--cardBg,#1a2035);
}

.dark-mode .marker-dot {
  border-color: var(--elevatedBg,#232d42);
  box-shadow: 0 2px 8px rgba(236, 72, 153, 0.3);
}

.dark-mode .life-stat-value {
  color: var(--accentPink,#F472B6);
}

.dark-mode .life-stat-divider {
  background: var(--borderDark,rgba(71,85,105,0.25));
}

.dark-mode .date-calc-toggle {
  background: var(--cardBg,#1a2035);
  color: var(--textSecondary,#94A3B8);
  border-color: var(--borderDark,rgba(71,85,105,0.25));
}

.dark-mode .date-calc-toggle.active {
  background: linear-gradient(135deg, #EC4899 0%, #DB2777 100%);
  color: #FFFFFF;
  border-color: transparent;
}

.dark-mode .date-calc-picker {
  background: var(--cardBg,#1a2035);
  color: var(--textPrimary,#F1F5F9);
}

.dark-mode .date-calc-result {
  background: var(--cardBg,#1a2035);
}

.dark-mode .calc-result-value {
  color: var(--accentPink,#F472B6);
}

.dark-mode .calc-detail-item {
  color: var(--textSecondary,#94A3B8);
}

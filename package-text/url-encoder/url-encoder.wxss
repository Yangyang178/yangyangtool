@import "../../styles/tool-common.wxss";

page { background-color: var(--pageBg,#F8FAFC); }

.tool-page {
  height: 100vh;
  display: flex;
  flex-direction: column;
}

.container {
  padding: 20px;
}

.tool-header {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 24px 0 20px;
}

.icon-wrapper {
  width: 72px;
  height: 72px;
  border-radius: 20px;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 12px;
}

.tool-icon {
  font-size: 36px;
}

.tool-title {
  font-size: 22px;
  font-weight: 700;
  color: var(--textPrimary,#1E293B);
  margin-bottom: 6px;
}

.tool-desc {
  font-size: 13px;
  color: var(--textSecondary,#64748B);
}

.mode-tabs {
  display: flex;
  gap: 10px;
  margin-bottom: 20px;
  background: white;
  padding: 6px;
  border-radius: 14px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
}

.mode-tab {
  flex: 1;
  padding: 12px 8px;
  border-radius: 10px;
  text-align: center;
  font-size: 13px;
  font-weight: 600;
  color: var(--textSecondary,#64748B);
  transition: all 0.3s ease;
}

.mode-tab.active {
  background: linear-gradient(135deg, var(--accentTeal,#14B8A6) 0%, var(--textPrimary,#1E293B) 100%);
  color: white;
  box-shadow: 0 4px 12px rgba(6, 182, 212, 0.4);
}

.input-section,
.output-section {
  background: white;
  border-radius: 16px;
  padding: 18px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
  margin-bottom: 16px;
}

.section-header-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}

.section-label {
  font-size: 15px;
  font-weight: 700;
  color: var(--textPrimary,#1E293B);
}

.input-actions,
.output-actions {
  display: flex;
  gap: 8px;
}

.action-tag {
  padding: 5px 12px;
  border-radius: 14px;
  font-size: 11px;
  font-weight: 600;
  transition: transform 0.2s ease;
}

.action-tag:active {
  transform: scale(0.95);
}

.action-tag.paste {
  background: var(--bgTipBlue,#EFF6FF);
  color: var(--accentBlue,#3B82F6);
}

.action-tag.sample {
  background: var(--bgTipGreen,#ECFDF5);
  color: var(--accentGreen,#10B981);
}

.action-tag.clear {
  background: var(--bgTipRed,#FEF2F2);
  color: var(--accentRed,#EF4444);
}

.action-tag.copy-main {
  background: linear-gradient(135deg, var(--accentTeal,#14B8A6) 0%, var(--textPrimary,#1E293B) 100%);
  color: white;
}

.url-input {
  width: 100%;
  min-height: 120px;
  max-height: 250px;
  background: var(--pageBg,#F8FAFC);
  border: 2px solid var(--borderLight,#E2E8F0);
  border-radius: 10px;
  padding: 14px;
  font-size: 13px;
  font-family: 'Courier New', monospace;
  color: var(--textPrimary,#1E293B);
  line-height: 1.7;
  box-sizing: border-box;
}

.url-input:focus {
  border-color: var(--accentTeal,#14B8A6);
}

.input-stats,
.output-stats {
  display: flex;
  gap: 16px;
  margin-top: 10px;
  padding-top: 10px;
  border-top: 1px solid var(--borderLight,#E2E8F0);
}

.stat-item {
  font-size: 12px;
  color: var(--textTertiary,#94A3B8);
  font-weight: 500;
}

.convert-btn {
  width: 100%;
  height: 52px;
  background: linear-gradient(135deg, var(--accentTeal,#14B8A6) 0%, var(--textPrimary,#1E293B) 100%);
  border-radius: 12px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 16px;
  font-weight: 700;
  color: white;
  margin-bottom: 16px;
  box-shadow: 0 4px 12px rgba(6, 182, 212, 0.4);
  transition: all 0.2s ease;
}

.convert-btn:active {
  transform: scale(0.98);
}

.output-container {
  background: var(--textPrimary,#1E293B);
  border-radius: 10px;
  overflow: hidden;
}

.output-scroll {
  max-height: 200px;
  padding: 14px;
}

.output-text {
  font-size: 13px;
  font-family: 'Courier New', monospace;
  color: var(--borderLight,#E2E8F0);
  line-height: 1.7;
  word-break: break-all;
  white-space: pre-wrap;
}

.parse-section {
  background: linear-gradient(135deg, var(--accentTeal,#14B8A6) 0%, var(--textPrimary,#1E293B) 100%);
  border-radius: 16px;
  padding: 20px;
  margin-bottom: 16px;
  box-shadow: 0 4px 16px rgba(6, 182, 212, 0.3);
}

.parse-title {
  font-size: 16px;
  font-weight: 700;
  color: white;
  display: block;
  margin-bottom: 14px;
}

.parse-card {
  background: rgba(255, 255, 255, 0.95);
  border-radius: 12px;
  padding: 16px;
}

.parse-item {
  display: flex;
  align-items: baseline;
  padding: 10px 0;
  border-bottom: 1px solid var(--borderLight,#E2E8F0);
}

.parse-item:last-child {
  border-bottom: none;
}

.parse-item.full-width {
  flex-direction: column;
  align-items: stretch;
}

.parse-label {
  font-size: 12px;
  color: var(--textSecondary,#64748B);
  font-weight: 600;
  min-width: 120px;
  flex-shrink: 0;
}

.parse-value {
  font-size: 13px;
  color: var(--textPrimary,#1E293B);
  font-family: 'Courier New', monospace;
  font-weight: 500;
  word-break: break-all;
  flex: 1;
}

.params-list {
  margin-top: 8px;
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.param-item {
  background: var(--bgTipBlue,#EFF6FF);
  border-radius: 8px;
  padding: 6px 10px;
  display: inline-flex;
  align-items: center;
  gap: 4px;
}

.param-key {
  font-size: 12px;
  font-weight: 700;
  color: var(--accentBlue,#3B82F6);
  font-family: 'Courier New', monospace;
}

.param-equal {
  font-size: 12px;
  color: var(--textTertiary,#94A3B8);
  font-weight: 600;
}

.param-value {
  font-size: 12px;
  color: var(--textPrimary,#1E293B);
  font-family: 'Courier New', monospace;
}

.param-table-section {
  margin-top: 14px;
}

.param-table-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 10px;
}

.param-table-title {
  font-size: 14px;
  font-weight: 700;
  color: var(--textPrimary,#1E293B);
}

.param-add-btn {
  padding: 4px 12px;
  border-radius: 8px;
  background: rgba(6,182,212,0.12);
  color: #06B6D4;
  font-size: 12px;
  font-weight: 700;
}

.param-table {
  border-radius: 10px;
  overflow: hidden;
  border: 1px solid var(--borderLight,#E2E8F0);
}

.param-table-row {
  display: flex;
  align-items: center;
  border-bottom: 1px solid var(--borderLight,#E2E8F0);
  min-height: 40px;
}

.param-table-row:last-child {
  border-bottom: none;
}

.param-table-head {
  background: var(--pageBg,#F8FAFC);
  font-weight: 700;
}

.pt-cell {
  padding: 8px 6px;
  font-size: 12px;
  display: flex;
  align-items: center;
}

.pt-idx {
  width: 30px;
  justify-content: center;
  color: var(--textTertiary,#94A3B8);
  font-weight: 600;
  flex-shrink: 0;
}

.pt-key {
  flex: 1;
  font-weight: 700;
  color: #3B82F6;
  font-family: 'Courier New', monospace;
  word-break: break-all;
  min-width: 0;
}

.pt-val {
  flex: 1.5;
  color: var(--textPrimary,#1E293B);
  font-family: 'Courier New', monospace;
  word-break: break-all;
  min-width: 0;
}

.pt-act {
  width: 60px;
  justify-content: center;
  gap: 6px;
  flex-shrink: 0;
}

.pt-act-edit {
  gap: 8px;
}

.pt-edit, .pt-del, .pt-save, .pt-cancel {
  font-size: 14px;
  padding: 2px;
}

.pt-save {
  color: #10B981;
  font-weight: 700;
  font-size: 16px;
}

.pt-cancel {
  color: #EF4444;
  font-weight: 700;
  font-size: 16px;
}

.pt-edit-cell {
  flex: 1;
  min-width: 0;
}

.pt-edit-input {
  width: 100%;
  height: 28px;
  border: 1.5px solid #06B6D4;
  border-radius: 6px;
  padding: 0 8px;
  font-size: 12px;
  font-family: 'Courier New', monospace;
  color: var(--textPrimary,#1E293B);
  background: #FFFFFF;
  box-sizing: border-box;
}

.rebuilt-url-section {
  margin-top: 14px;
}

.rebuilt-label {
  font-size: 13px;
  font-weight: 700;
  color: var(--textSecondary,#64748B);
  display: block;
  margin-bottom: 8px;
}

.rebuilt-url-box {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 10px 12px;
  background: rgba(255,255,255,0.6);
  border-radius: 8px;
  border: 1px solid var(--borderLight,#E2E8F0);
}

.rebuilt-url-text {
  flex: 1;
  font-size: 12px;
  font-family: 'Courier New', monospace;
  color: var(--textPrimary,#1E293B);
  word-break: break-all;
  line-height: 1.5;
}

.rebuilt-copy-btn {
  font-size: 16px;
  flex-shrink: 0;
  padding: 4px;
}

.short-url-section {
  margin-top: 14px;
  padding-top: 14px;
  border-top: 1px solid rgba(255,255,255,0.2);
}

.short-url-header {
  margin-bottom: 10px;
}

.short-url-label {
  font-size: 14px;
  font-weight: 700;
  color: #FFFFFF;
}

.short-url-btn {
  height: 40px;
  border-radius: 10px;
  background: rgba(255,255,255,0.2);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 14px;
  font-weight: 700;
  color: #FFFFFF;
  border: 1px solid rgba(255,255,255,0.3);
}

.short-url-btn:active {
  background: rgba(255,255,255,0.3);
}

.short-url-result {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-top: 10px;
  padding: 10px 12px;
  background: rgba(255,255,255,0.15);
  border-radius: 8px;
}

.short-url-text {
  flex: 1;
  font-size: 13px;
  font-family: 'Courier New', monospace;
  color: #FFFFFF;
  word-break: break-all;
}

.short-url-copy {
  font-size: 16px;
  flex-shrink: 0;
  padding: 4px;
}

.reference-section {
  background: white;
  border-radius: 16px;
  padding: 18px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
  margin-bottom: 16px;
}

.ref-title {
  font-size: 15px;
  font-weight: 700;
  color: var(--textPrimary,#1E293B);
  display: block;
  margin-bottom: 12px;
}

.ref-table {
  overflow-x: auto;
}

.ref-header,
.ref-row {
  display: grid;
  grid-template-columns: 60px 100px 1fr;
  gap: 10px;
  padding: 10px 8px;
  border-bottom: 1px solid var(--borderLight,#E2E8F0);
}

.ref-header {
  background: var(--pageBg,#F8FAFC);
  border-radius: 8px;
  font-weight: 700;
}

.ref-row:last-child {
  border-bottom: none;
}

.ref-cell {
  font-size: 12px;
  color: var(--textPrimary,#1E293B);
  display: flex;
  align-items: center;
}

.ref-cell.header {
  color: var(--textSecondary,#64748B);
  font-size: 11px;
}

.ref-cell.char {
  font-size: 16px;
  font-weight: 700;
  justify-content: center;
}

.ref-cell.encoded {
  font-family: 'Courier New', monospace;
  color: var(--textPrimary,#1E293B);
  font-weight: 600;
}

.ref-cell.desc {
  color: var(--textSecondary,#64748B);
  font-size: 11px;
}

.tips-section {
  background: white;
  border-radius: 16px;
  padding: 18px;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.06);
}

.tips-title {
  font-size: 15px;
  font-weight: 700;
  color: var(--textPrimary,#1E293B);
  display: block;
  margin-bottom: 12px;
}

.tips-list {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.tip-item {
  display: flex;
  gap: 8px;
  align-items: flex-start;
}

.tip-bullet {
  color: var(--accentTeal,#14B8A6);
  font-weight: 700;
  font-size: 16px;
  line-height: 1.5;
}

.tip-text {
  font-size: 13px;
  color: var(--textSecondary,#64748B);
  line-height: 1.6;
}

/* ===================================
   🌙 夜间模式完整适配 - 青色主题
   =================================== */
.dark-mode page {
  background-color: var(--pageBgNight,#0f1419);
}

.dark-mode .mode-tabs {
  background: var(--elevatedBg,#232d42);
  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.15);
}

.dark-mode .input-section,
.dark-mode .output-section {
  background: var(--elevatedBg,#232d42);
  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.15);
}

.dark-mode .section-label {
  color: var(--textPrimary,#F1F5F9);
}

.dark-mode .action-tag.paste {
  background: rgba(59, 130, 246, 0.12);
  color: var(--accentBlue,#60A5FA);
}

.dark-mode .action-tag.sample {
  background: rgba(16, 185, 129, 0.12);
  color: var(--accentGreen,#34D399);
}

.dark-mode .action-tag.clear {
  background: rgba(239, 68, 68, 0.12);
  color: var(--accentRed,#F87171);
}

.dark-mode .action-tag.copy-main {
  background: linear-gradient(135deg, #06B6D4 0%, #0891B2 100%);
  color: white;
}

.dark-mode .input-stats,
.dark-mode .output-stats {
  border-top-color: var(--borderLight,#374151);
}

.dark-mode .icon-wrapper {
  box-shadow: 0 8px 24px rgba(6, 182, 212, 0.25);
}

.dark-mode .tool-title {
  color: var(--textPrimary,#F1F5F9);
}

.dark-mode .tool-desc {
  color: var(--textSecondary,#94A3B8);
}

.dark-mode .mode-tab {
  background: var(--cardBg,#1a2035);
  color: var(--textSecondary,#94A3B8);
}

.dark-mode .mode-tab.active {
  background: linear-gradient(135deg, #06B6D4 0%, #0891B2 100%);
  box-shadow: 0 4px 12px rgba(6, 182, 212, 0.25);
}

.dark-mode .input-label {
  color: var(--textPrimary,#F1F5F9);
}

.dark-mode .url-input {
  background: var(--cardBg,#1a2035);
  color: var(--textPrimary,#F1F5F9);
  border-color: var(--borderLight,#374151);
}

.dark-mode .convert-btn {
  box-shadow: 0 4px 12px rgba(6, 182, 212, 0.3);
}

.dark-mode .output-container {
  background: #0f1419;
  border: 1px solid var(--borderLight,#374151);
}

.dark-mode .output-text {
  color: var(--borderLight,#CBD5E1);
}

.dark-mode .parse-section {
  background: linear-gradient(135deg, #0e7490 0%, #0f1419 100%);
  box-shadow: 0 4px 16px rgba(6, 182, 212, 0.2);
}

.dark-mode .parse-card {
  background: rgba(26, 32, 53, 0.95);
}

.dark-mode .parse-item {
  border-bottom-color: var(--borderLight,#374151);
}

.dark-mode .parse-label {
  color: var(--textSecondary,#94A3B8);
}

.dark-mode .parse-value {
  color: var(--textPrimary,#F1F5F9);
}

.dark-mode .param-item {
  background: rgba(59, 130, 246, 0.12);
}

.dark-mode .param-key {
  color: var(--accentBlue,#60A5FA);
}

.dark-mode .param-value {
  color: var(--textPrimary,#F1F5F9);
}

.dark-mode .param-table-title {
  color: var(--textPrimary,#F1F5F9);
}

.dark-mode .param-add-btn {
  background: rgba(6,182,212,0.18);
  color: #22D3EE;
}

.dark-mode .param-table {
  border-color: var(--borderLight,#374151);
}

.dark-mode .param-table-row {
  border-bottom-color: var(--borderLight,#374151);
}

.dark-mode .param-table-head {
  background: var(--cardBg,#1a2035);
}

.dark-mode .pt-key {
  color: #60A5FA;
}

.dark-mode .pt-val {
  color: var(--textPrimary,#F1F5F9);
}

.dark-mode .pt-edit-input {
  background: var(--cardBg,#1a2035);
  color: var(--textPrimary,#F1F5F9);
  border-color: #22D3EE;
}

.dark-mode .rebuilt-label {
  color: var(--textSecondary,#94A3B8);
}

.dark-mode .rebuilt-url-box {
  background: rgba(26,32,53,0.6);
  border-color: var(--borderLight,#374151);
}

.dark-mode .rebuilt-url-text {
  color: var(--textPrimary,#F1F5F9);
}

.dark-mode .reference-section {
  background: var(--elevatedBg,#232d42);
  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.15);
}

.dark-mode .ref-title {
  color: var(--textPrimary,#F1F5F9);
}

.dark-mode .ref-header {
  background: var(--cardBg,#1a2035);
}

.dark-mode .ref-row {
  border-bottom-color: var(--borderLight,#374151);
}

.dark-mode .ref-cell {
  color: var(--textPrimary,#F1F5F9);
}

.dark-mode .ref-cell.header {
  color: var(--textSecondary,#94A3B8);
}

.dark-mode .tips-section {
  background: var(--elevatedBg,#232d42);
  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.15);
}

.dark-mode .tips-title {
  color: var(--textPrimary,#F1F5F9);
}

.dark-mode .tip-bullet {
  color: var(--accentTeal,#2DD4BF);
}

.dark-mode .tip-text {
  color: var(--textSecondary,#94A3B8);
}